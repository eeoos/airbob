# ============================================================
# AWS (x86_64) 배포용 환경변수
# 사용법: cp .env.aws.example .env.aws && vim .env.aws
#
# AWS 자격증명은 IAM Role/Instance Profile에서 자동 주입
# 변수명은 application-aws.yaml의 ${...}과 정확히 일치해야 함
# ============================================================

# ------------------------------------------------------------
# Database (MySQL / RDS)
# ------------------------------------------------------------
SPRING_DATASOURCE_URL=jdbc:mysql://your-rds-endpoint:3306/airbobdb
SPRING_DATASOURCE_USERNAME=airbob
SPRING_DATASOURCE_PASSWORD=your_app_db_password

# ------------------------------------------------------------
# Redis (ElastiCache)
# ------------------------------------------------------------
REDIS_HOST=your-elasticache-endpoint

# ------------------------------------------------------------
# Elasticsearch (OpenSearch)
# ------------------------------------------------------------
ELASTICSEARCH_URIS=https://your-opensearch-endpoint:443
ELASTICSEARCH_USERNAME=
ELASTIC_PASSWORD=

# ------------------------------------------------------------
# Kafka (MSK)
# ------------------------------------------------------------
KAFKA_BOOTSTRAP_SERVERS=your-msk-broker1:9092,your-msk-broker2:9092

# ------------------------------------------------------------
# AWS S3 & CloudFront (자격증명은 IAM Role에서 자동 주입)
# ------------------------------------------------------------
AWS_S3_BUCKET_NAME=your_s3_bucket_name
AWS_REGION=ap-northeast-2
CLOUDFRONT_DOMAIN=your_cloudfront_domain.cloudfront.net

# ------------------------------------------------------------
# External APIs
# ------------------------------------------------------------
GOOGLE_API_KEY=your_google_api_key
IPINFO_API_TOKEN=your_ipinfo_token
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx
TOSS_SECRET_KEY=your_toss_secret_key
