# ============================================================
# 로컬 개발용 환경변수 (선택사항)
# 사용법: cp .env.example .env && vim .env
#
# 대부분의 값은 application-dev.yaml에 기본값이 있음
# AWS S3 사용 시에만 아래 값들 설정 필요
# ============================================================

# ------------------------------------------------------------
# AWS S3 & CloudFront (이미지 업로드 테스트 시 필요)
# ------------------------------------------------------------
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_S3_BUCKET_NAME=your_s3_bucket_name
AWS_REGION=ap-northeast-2
CLOUDFRONT_DOMAIN=your_cloudfront_domain.cloudfront.net

# ------------------------------------------------------------
# External APIs (선택사항)
# ------------------------------------------------------------
GOOGLE_API_KEY=your_google_api_key
IPINFO_API_TOKEN=your_ipinfo_token
SLACK_WEBHOOK_URL=
TOSS_SECRET_KEY=your_toss_secret_key
